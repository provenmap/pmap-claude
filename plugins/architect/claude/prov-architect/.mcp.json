{
  "mcpServers": {
    "provenmap": {
      "type": "http",
      "url": "${PROV_MCP_URL:-https://platform.provenmap.com/api/mcp}",
      "headers": {
        "Authorization": "Bearer ${PROV_MCP_TOKEN}"
      }
    }
  }
}
